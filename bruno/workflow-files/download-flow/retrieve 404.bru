meta {
  name: retrieve 404
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/workflow-files/{{file-upload-flow-invalid-uuid}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
