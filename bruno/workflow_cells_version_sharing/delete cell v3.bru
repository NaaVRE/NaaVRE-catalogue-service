meta {
  name: delete cell v3
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/workflow-cells/{{shared-cell-test-user-2-v3-uuid}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
