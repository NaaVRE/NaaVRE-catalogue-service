meta {
  name: test-user-2 link cell v1 to v2
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/workflow-cells/{{shared-cell-test-user-2-v1-uuid}}/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

body:json {
  {
    "next_version": "{{baseUrl}}/workflow-cells/{{shared-cell-test-user-2-v2-uuid}}/"
  }
}

tests {
  test("Status code is 201", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
