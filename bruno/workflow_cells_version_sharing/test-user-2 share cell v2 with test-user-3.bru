meta {
  name: test-user-2 share cell v2 with test-user-3
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/workflow-cells/{{shared-cell-test-user-2-v2-uuid}}/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

body:json {
  {
    "shared_with_scopes": [],
    "shared_with_users": [
      "test-user-3"
    ]
  }
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
