meta {
  name: List user jwt
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/binder-environments/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
