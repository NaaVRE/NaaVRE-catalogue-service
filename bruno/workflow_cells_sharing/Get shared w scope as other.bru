meta {
  name: Get shared w scope as other
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/workflow-cells/{{shared-cell-test-user-2-uuid}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{thirdJWT}}
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
