meta {
  name: Delete cell-2
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/workflow-cells/{{cell-2-uuid}}/
  body: none
  auth: inherit
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
