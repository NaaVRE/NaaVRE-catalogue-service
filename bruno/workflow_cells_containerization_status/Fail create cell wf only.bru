meta {
  name: Fail create cell wf only
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/workflow-cells/
  body: json
  auth: inherit
}

body:json {
  {
    "virtual_lab": "test-virtual-lab-1",
    "title": "cell-fail",
    "container_image": "image",
    "containerization_workflow_id": "b31bb840-124a-42ec-85ec-972e9d23de45"
  }
}

tests {
  test("Status code", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
