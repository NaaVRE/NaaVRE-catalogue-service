meta {
  name: Delete cell-3
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/workflow-cells/{{cell-3-uuid}}/
  body: none
  auth: inherit
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
