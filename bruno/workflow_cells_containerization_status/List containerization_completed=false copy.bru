meta {
  name: List containerization_completed=false copy
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/workflow-cells/?containerization_completed=false
  body: none
  auth: inherit
}

params:query {
  containerization_completed: false
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
  test("JSON response is valid", function () {
      var jsonData = res.getBody();
      expect(jsonData.count).to.eq(0);
  });
}
