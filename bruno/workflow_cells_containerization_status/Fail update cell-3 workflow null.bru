meta {
  name: Fail update cell-3 workflow null
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/workflow-cells/{{cell-3-uuid}}/
  body: json
  auth: inherit
}

body:json {
  {
    "containerization_workflow_id": null
  }
}

tests {
  test("Status code", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
}
