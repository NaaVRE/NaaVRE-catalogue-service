meta {
  name: Fail update cell-1 wf only
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/workflow-cells/{{cell-1-uuid}}/
  body: json
  auth: inherit
}

body:json {
  {
    "containerization_workflow_id": "b31bb840-124a-42ec-85ec-972e9d23de45"
  }
}

tests {
  test("Status code", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
}
