meta {
  name: Delete draft
  type: http
  seq: 26
}

delete {
  url: {{baseUrl}}/workflow-cells/{{draftCellUUID}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
