meta {
  name: Delete other new version
  type: http
  seq: 27
}

delete {
  url: {{baseUrl}}/workflow-cells/{{otherValidCellUUIDv2}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
