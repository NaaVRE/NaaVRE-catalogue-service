meta {
  name: create duplicate
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/notebook-files/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

body:json {
  {
    "virtual_lab": "test-virtual-lab-1",
    "title": "notebook-file-1",
    "key": "{{file-upload-flow-key}}"
  }
  
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
}
