meta {
  name: list unauthenticated
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/notebook-files/
  body: none
  auth: none
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
