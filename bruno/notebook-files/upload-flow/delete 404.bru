meta {
  name: delete 404
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/notebook-files/{{file-upload-flow-invalid-uuid}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
