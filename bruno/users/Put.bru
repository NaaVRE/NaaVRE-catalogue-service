meta {
  name: Put
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/users/4/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

body:json {
  {}
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(405);
  });
}
